<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.tamuri</groupId>
    <artifactId>swmutsel</artifactId>
    <packaging>jar</packaging>
    <version>2.0-SNAPSHOT</version>
    <name>swmutsel</name>
    <url>https://github.com/tamuri/swmutsel</url>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
    </properties>

    <dependencies>
        <dependency>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
            <version>1.1.3</version>
        </dependency>
        <dependency>
            <groupId>colt</groupId>
            <artifactId>colt</artifactId>
            <version>1.2.0</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math3</artifactId>
            <version>3.2</version>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>15.0</version>
        </dependency>
        <dependency>
            <groupId>com.caucho</groupId>
            <artifactId>hessian</artifactId>
            <version>4.0.38</version>
        </dependency>
        <dependency>
            <groupId>com.beust</groupId>
            <artifactId>jcommander</artifactId>
            <version>1.32</version>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>servlet-api</artifactId>
            <version>2.5</version>
        </dependency>
        <dependency>
            <groupId>org.jgrapht</groupId>
            <artifactId>jgrapht-core</artifactId>
            <version>0.9.1</version>
        </dependency>
        <!-- Build local copy of PAL and install into local Maven repo:
        git clone https://github.com/tamuri/pal.git
        cd pal
        git checkout tamuri
        ant
        mvn install:install-file \
            -DgroupId=org.pal-project \
            -Dpackaging=jar -DgeneratePom=true \
            -DartifactId=pal \
            -Dfile=dist/lib/pal-tamuri-2511251146.jar \
            -Dversion=tamuri-2511251146
        -->
        <dependency>
          <groupId>org.pal-project</groupId>
          <artifactId>pal</artifactId>
          <version>tamuri-2511251146</version>
        </dependency>
        <!-- Build local copy (requires Linux) and install into local Maven repo:
        git clone https://github.com/mkobos/lbfgsb_wrapper.git
        cd lbfgsb_wrapper
        ant
        mvn install:install-file \
            -DgroupId=mkobos \
            -DartifactId=lbfgsb_wrapper \
            -Dpackaging=jar -DgeneratePom=true \
            -Dfile=dist/lbfgsb_wrapper-1.1.5.jar \
            -Dversion=1.1.5
        -->
        <dependency>
            <groupId>mkobos</groupId>
            <artifactId>lbfgsb_wrapper</artifactId>
            <version>1.1.5</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>project</id>
            <url>file://${project.basedir}/repo</url>
        </repository>
    </repositories>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>swmutsel.Run</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                </configuration>
                        <executions>
          <execution>
            <id>make-assembly</id> <!-- this is used for inheritance merges -->
            <phase>package</phase> <!-- bind to the packaging phase -->
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
            </plugin>
        </plugins>
    </build>
</project>

